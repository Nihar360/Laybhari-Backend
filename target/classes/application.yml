server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:${DB_URL:jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:${MYSQL_DATABASE:laybhari_db}}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true}}
    username: ${MYSQLUSER:${SPRING_DATASOURCE_USERNAME:${DB_USERNAME:root}}}
    password: ${MYSQLPASSWORD:${SPRING_DATASOURCE_PASSWORD:${MYSQL_ROOT_PASSWORD:${DB_PASSWORD:nihar@360}}}}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # schema managed via schema.sql
    show-sql: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:laybhari_jwt_super_secret_key_minimum_32_characters_long_2026}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}   # 24 hours

razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_TJmHXuuJ0nSYDV}
  key-secret: ${RAZORPAY_KEY_SECRET:ki8x4Sgr76UnNrHBnD85uanY}

cors:
  allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://localhost:4173,https://*.vercel.app,*}
